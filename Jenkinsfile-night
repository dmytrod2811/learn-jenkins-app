pipeline {
    agent any

    stages {
        stage('Docker image build') {
            steps {
                sh '''
                    docker build -f ci/Dockerfile -t my-playwright .
                '''
            }
        }
    }
}